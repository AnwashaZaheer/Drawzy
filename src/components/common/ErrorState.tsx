import { AlertCircle, Home, RotateCcw } from 'lucide-react';
import { Button } from './Button';
type ErrorKind = 'general' | 'room';
export const ErrorState = ({ kind = 'general', onRetry, onBackHome }: { kind?: ErrorKind; onRetry?: () => void; onBackHome?: () => void }) => { const room = kind === 'room'; return <div className="flex flex-col items-center justify-center rounded-3xl border border-red-100 bg-white p-8 text-center shadow-sm" role="alert"><div className="mb-4 rounded-2xl bg-red-50 p-3 text-red-500"><AlertCircle className="h-7 w-7" /></div><h2 className="text-xl font-extrabold text-gray-900">{room ? 'Room not found' : 'Something went wrong'}</h2><p className="mt-2 text-sm font-medium text-gray-500">{room ? 'Check the room code and try again.' : 'Please try again.'}</p>{room ? <Button variant="secondary" onClick={onBackHome} className="mt-5 gap-2"><Home className="h-4 w-4" />Back to Home</Button> : <Button onClick={onRetry} className="mt-5 gap-2"><RotateCcw className="h-4 w-4" />Try Again</Button>}</div>; };
