import { CheckCircle2, Users } from 'lucide-react';
export interface GameSettings { maxPlayers: number; rounds: number; drawingTime: number; wordChoices: number; language: string; isPrivate: boolean; }
export const GameSummary = ({ settings }: { settings: GameSettings }) => { const items = [`${settings.maxPlayers} Players`, `${settings.rounds} ${settings.rounds === 1 ? 'Round' : 'Rounds'}`, `${settings.drawingTime} Seconds`, `${settings.wordChoices} Words`, settings.language, settings.isPrivate ? 'Private Game' : 'Public Game']; return <aside className="h-fit rounded-3xl border border-indigo-100 bg-indigo-50/70 p-5 sm:p-6"><div className="mb-4 flex items-center gap-2 text-indigo-700"><div className="rounded-xl bg-white p-2 shadow-sm"><Users className="h-4 w-4" /></div><h2 className="text-lg font-extrabold">Game Summary</h2></div><ul className="space-y-3">{items.map((item) => <li key={item} className="flex items-center gap-2.5 text-sm font-bold text-gray-700"><CheckCircle2 className="h-4 w-4 shrink-0 text-indigo-600" aria-hidden="true" />{item}</li>)}</ul></aside>; };
