interface ToggleSettingProps { checked: boolean; onChange: (checked: boolean) => void; }
export const ToggleSetting = ({ checked, onChange }: ToggleSettingProps) => <div className="flex items-center justify-between gap-4 rounded-2xl border border-gray-100 bg-gray-50 p-4"><div><p className="font-extrabold text-gray-800">Private Game</p><p className="mt-1 text-sm font-medium leading-5 text-gray-500">Only players with your room code can join.</p></div><button type="button" role="switch" aria-checked={checked} aria-label="Private Game" onClick={() => onChange(!checked)} className={`relative h-7 w-12 shrink-0 rounded-full transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 ${checked ? 'bg-indigo-600' : 'bg-gray-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow-sm transition-transform ${checked ? 'translate-x-6' : 'translate-x-1'}`} /></button></div>;
