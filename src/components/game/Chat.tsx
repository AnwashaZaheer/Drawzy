import { useEffect, useRef } from 'react'; import { ChatMessage, type ChatEntry } from './ChatMessage'; import { GuessInput } from './GuessInput'; import { EmptyState } from '../common/EmptyState';
export const Chat = ({ messages, onSend }: { messages: ChatEntry[]; onSend: (text: string) => void }) => { const endRef=useRef<HTMLDivElement>(null); useEffect(()=>endRef.current?.scrollIntoView({behavior:'smooth'}),[messages]); return <aside className="flex min-h-[360px] flex-col overflow-hidden rounded-3xl border border-gray-100 bg-white shadow-sm lg:min-h-0"><div className="border-b border-gray-100 px-5 py-4"><h2 className="font-extrabold text-gray-900">Guesses</h2></div><div className="flex-1 space-y-3 overflow-y-auto p-4">{messages.length === 0 ? <EmptyState kind="chat" /> : messages.map((message)=><ChatMessage key={message.id} message={message} />)}<div ref={endRef} /></div><GuessInput onSend={onSend} /></aside>; };
