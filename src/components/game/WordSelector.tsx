const words = [{ word: 'Pizza', emoji: '🍕' }, { word: 'Rocket', emoji: '🚀' }, { word: 'House', emoji: '🏠' }];
export const WordSelector = ({ onSelect }: { onSelect: (word: string) => void }) => <section className="rounded-3xl border border-yellow-200 bg-yellow-50 p-5 text-center shadow-sm"><h2 className="text-xl font-extrabold text-gray-900">Choose a word</h2><div className="mt-4 grid gap-3 sm:grid-cols-3">{words.map(({ word, emoji }) => <button key={word} type="button" onClick={() => onSelect(word)} className="rounded-2xl border border-yellow-200 bg-white px-4 py-5 text-lg font-extrabold text-gray-800 transition-all hover:-translate-y-0.5 hover:border-indigo-300 hover:shadow-md active:scale-[0.98] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">{emoji} {word}</button>)}</div></section>;
