import type { Player } from './types';
export const PlayerCard = ({ player }: { player: Player }) => <article className="paper-card p-4 rotate-0 hover:rotate-1 hover:scale-[1.02] transition-transform"><div className="flex items-start justify-between"><span className="text-3xl">{player.avatar}</span><span className={`h-3 w-3 rounded-full border-2 border-ink ${player.isReady ? 'bg-crayon-green' : 'bg-crayon-yellow'}`} /></div><div className="mt-3 flex items-center gap-2"><h3 className="font-bold text-ink">{player.name}</h3>{player.isHost && <span className="rounded-lg bg-crayon-yellow border border-ink px-2 py-0.5 text-xs font-extrabold text-ink -rotate-2">Host</span>}</div><p className={`mt-1 text-xs font-bold ${player.isReady ? 'text-crayon-green' : 'text-crayon-orange'}`}>{player.isReady ? 'ready!' : 'not ready...'}</p></article>;