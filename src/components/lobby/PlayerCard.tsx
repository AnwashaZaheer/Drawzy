import type { Player } from './types';
export const PlayerCard = ({ player }: { player: Player }) => <article className="rounded-2xl border border-gray-100 bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><span className="text-3xl">{player.avatar}</span><span className={`h-2.5 w-2.5 rounded-full ${player.isReady ? 'bg-emerald-500' : 'bg-amber-400'}`} /></div><div className="mt-3 flex items-center gap-2"><h3 className="font-extrabold text-gray-800">{player.name}</h3>{player.isHost && <span className="rounded-lg bg-yellow-100 px-2 py-0.5 text-xs font-extrabold text-yellow-700">Host</span>}</div><p className={`mt-1 text-xs font-bold ${player.isReady ? 'text-emerald-600' : 'text-amber-600'}`}>{player.isReady ? 'Ready' : 'Not Ready'}</p></article>;
