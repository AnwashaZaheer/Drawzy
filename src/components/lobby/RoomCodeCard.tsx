import { Copy, Check } from 'lucide-react';
import { Button } from '../common/Button';
export const RoomCodeCard = ({ code, copied, onCopy }: { code: string; copied: boolean; onCopy: () => void }) => <section className="paper-card p-6 text-center sm:p-8 rotate-[-0.5deg]"><p className="text-xs font-extrabold tracking-[0.22em] text-ink/60">ROOM CODE · SHARE IT</p><p className="my-3 text-4xl font-extrabold tracking-[0.18em] text-ink sm:text-5xl bg-crayon-yellow/40 border-2 border-ink rounded-2xl inline-block px-4 py-1">{code}</p><p className="mb-5 text-sm font-medium text-ink/60">paste this code to your friends so they can join</p><Button variant={copied ? 'green' : 'primary'} onClick={onCopy} className="gap-2">{copied ? <Check className="h-4 w-4" /> : <Copy className="h-4 w-4" />}{copied ? 'Copied!' : 'Copy Code'}</Button></section>;