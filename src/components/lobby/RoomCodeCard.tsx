import { Copy, Check } from 'lucide-react';
import { Button } from '../common/Button';
export const RoomCodeCard = ({ code, copied, onCopy }: { code: string; copied: boolean; onCopy: () => void }) => <section className="rounded-3xl border border-indigo-100 bg-indigo-50/70 p-6 text-center shadow-sm sm:p-8"><p className="text-xs font-extrabold tracking-[0.22em] text-indigo-600">ROOM CODE</p><p className="my-3 text-4xl font-extrabold tracking-[0.18em] text-gray-900 sm:text-5xl">{code}</p><p className="mb-5 text-sm font-medium text-gray-600">Share this code with your friends</p><Button variant={copied ? 'secondary' : 'primary'} onClick={onCopy} className="gap-2">{copied ? <Check className="h-4 w-4" /> : <Copy className="h-4 w-4" />}{copied ? 'Copied!' : 'Copy Code'}</Button></section>;
